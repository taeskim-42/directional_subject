import 'package:flutter/widgets.dart';

class GameRecordPage extends StatelessWidget {
  const GameRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("this is GameRecord");
  }
}
